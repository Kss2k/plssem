% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bpls.R
\name{bpls}
\alias{bpls}
\title{Bayesian Partial Least Squares Structural Equation Modeling}
\usage{
bpls(
  syntax,
  data,
  ...,
  chains = 1L,
  iter = 2000,
  warmup = floor(iter/2),
  parallel = c("no", "multicore", "multisession", "snow"),
  ncores = chains,
  iseed = stats::runif(1, min = 1e+05, max = 999999),
  sampler = c("Metropolis-Hastings", "Gibbs"),
  verbose = interactive(),
  point.estimate = c("median", "mean"),
  warm.start = TRUE,
  noise.correction = TRUE,
  noise.correction.R = min(max(floor(warmup/4L), 200), 1000),
  bootstrap = NULL,
  consistent = FALSE,
  mcpls = NULL,
  probit = FALSE,
  rng.s.start = 0.2,
  rng.tune.pct = 0.1,
  rng.acceptance.rate = 0.44,
  mc.reps = 20000,
  acceptance.rate = function(d) 0.234 + 0.21/d,
  Q = list(r = function(x, s) as.vector(mvnfast::rmvn(n = 1, mu = x, sigma = s)), d =
    function(x, y, s, log = TRUE) mvnfast::dmvn(matrix(y, nrow = 1), mu = x, sigma = s,
    log = log))
)
}
\arguments{
\item{syntax}{Character string with \code{lavaan}-style model syntax describing
both measurement (\code{=~}) and structural (\code{~}) relations. Random effects are
specified with \code{(term | cluster)} statements.}

\item{data}{A \code{data.frame} or coercible object containing the manifest
indicators referenced in \code{syntax}. Ordered factors are automatically
detected, but can also be supplied explicitly through \code{ordered}.}

\item{...}{Additional arguments passed to [pls()], such as `ordered` and
`boot.R`.}

\item{chains}{Positive integer giving the number of MCMC chains.}

\item{iter}{Positive integer giving the total number of iterations per chain,
including warmup.}

\item{warmup}{Non-negative integer giving the number of initial iterations
discarded from each chain.}

\item{parallel}{The type of parallel operation to be used (if any). The
default is \code{"no"}. \code{"multisession"} runs the chains using multiple
background \code{R} sessions (works on all platforms), while \code{"multicore"}
uses forked processes (not available on Windows). \code{"snow"} is kept for
backwards compatibility and is treated as an alias for \code{"multisession"}.
Internally this is implemented using the \code{future} package}

\item{ncores}{Positive integer giving the number of parallel workers.}

\item{iseed}{Integer seed used for the chains}

\item{sampler}{MCMC blocking scheme. `"Metropolis-Hastings"` proposes all
parameters jointly; `"Gibbs"` uses groups of related parameters as blocks.}

\item{verbose}{Should verbose output be printed?}

\item{point.estimate}{Posterior point estimate used in the returned model.
Either `"median"` or `"mean"`.}

\item{warm.start}{Logical; use an MC-PLS fit to initialize the sampler and its
proposal covariance? Defaults to \code{TRUE}.}

\item{noise.correction}{Logical; estimate and correct for Monte Carlo noise in
the synthetic likelihood?}

\item{noise.correction.R}{Positive integer giving the number of simulations
used to estimate Monte Carlo noise.}

\item{bootstrap}{Reserved argument. Bootstrap estimation is performed
internally because it is required to construct the synthetic likelihood.}

\item{consistent}{Logical; request the PLSc consistency correction in the
underlying PLS fits. Should in general be set to \code{FALSE}.}

\item{mcpls}{Reserved argument; MC-PLS use is controlled by `warm.start`.}

\item{probit}{Logical; use probit factor scores in the underlying PLS fits?
Should in general be set to \code{FALSE}.}

\item{rng.s.start}{Initial scale used when correlating proposed and current
simulation innovations. For advanced users.}

\item{rng.tune.pct}{Fraction determining how often the innovation scale is
tuned during warmup. For advanced users.}

\item{rng.acceptance.rate}{Target acceptance rate for innovation proposals.
For advanced users.}

\item{mc.reps}{Positive integer giving the simulated sample size used to approximate
the likelihood.}

\item{acceptance.rate}{Function mapping a proposal-block dimension to its
target acceptance rate. For advanced users.}

\item{Q}{List with functions `r` and `d` for drawing from and evaluating the
parameter proposal distribution. For advanced users.}
}
\value{
A fitted `PlsModel` object. Posterior draws are available in the
  model's bootstrap results, including retained, warmup, and per-chain draws.
}
\description{
Fits a Bayesian PLS-SEM model using a pseudo-marginal Markov chain Monte
Carlo sampler. The estimator is an extension of the MC-PLSc estimator.
Priors can be specified directly for parameters in the model syntax, or
specified on parameter labels with the `:~` operator.
}
\examples{
\dontrun{
m <- '
  X =~ load * x1 + load * x2 + load * x3
  Z =~ load * z1 + load * z2 + load * z3
  Y =~ load * y1 + load * y2 + load * y3

  Y ~ "dnorm(.4, .1)" * X +
     "dnorm(.35, .1)" * Z +
     "dnorm(.45, .1)" * X:Z +
     "dnorm(0, .005)" * X:X

  load :~ dnorm(.8, .5)
'

set.seed(23942)
fit <- bpls(
  m, modsem::oneInt, boot.R = 500, warmup = 5000, iter = 10000,
  parallel = "multisession", chains = 2
)

summary(fit)
}

}
\seealso{
[pls()]
}
